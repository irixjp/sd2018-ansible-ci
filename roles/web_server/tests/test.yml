- name: unit test for the role
  hosts: all
  vars:
    role_name: web_server
  tasks:
    - name: Run role tasks from "{{ item }}"
      include_role:
        name: web_server
        tasks_from: "{{ item }}"
      with_items:
        - main
        - unit_test
