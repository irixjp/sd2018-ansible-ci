- name: unit test for the role
  hosts: all
  vars:
    role_name: web_server
  tasks:
    - include_role:
        name: web_server
        tasks_from: "{{ item }}"
      with_items:
        - main
        - unit_test
